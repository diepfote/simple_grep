Simple grep
===========

Simple grep is an alternative implementation of grep, fully written in python.
Most notable alteration: No file or directory specified implies searching in current directory.
Missing features: Currently no stdin support for Windows. 

Simple grep is sure to run on:
* CPython 2.7 and 3.6.
